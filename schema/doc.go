// Package schema declares and renders portable database schema operations.
//
// It deliberately contains no Domainry Record conventions. Callers that need
// the canonical workspace and audit columns should use package recordschema.
package schema
